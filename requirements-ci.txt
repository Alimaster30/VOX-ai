# Lightweight dependencies for automated tests.
#
# The production requirements include GPU/model/audio packages that are too
# heavy for normal CI. These packages cover the API, upload validation,
# persistence, backup/restore, deployment artifact, and monitoring tests.

flask>=3.0,<4
pytest>=8.0,<9
psycopg[binary]>=3.2,<4
pypdf>=6.0,<7
pandas>=2.2
openpyxl>=3.1,<4
waitress>=3.0,<4
